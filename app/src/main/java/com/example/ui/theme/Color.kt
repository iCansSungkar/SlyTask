package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Sleek Neo-Bento Dark Mode Vibe
val ObsidianBg = Color(0xFF08090C)         // Deep pure obsidian space background
val BentoCoal = Color(0xFF13151D)          // Charcoal bento-box surface card background
val BentoCardGlow = Color(0xFF1B1E28)      // Elevated card container depth
val NeonMint = Color(0xFF00FFCC)           // Cyber-electric neon mint for buttons & states
val NeonMintDim = Color(0xFF00B38F)        // Desaturated mint
val SoftCyan = Color(0xFF00E5FF)           // High tech soft cyan secondary highlight
val SoftCyanDim = Color(0xFF00A2B4)

val SlateTextPrimary = Color(0xFFF1F3F5)   // Off-white crisp typography
val SlateTextSecondary = Color(0xFF8E95A5) // Soft futuristic low-contrast grey for descriptions

val BorderHighTech = Color(0xFF222634)     // Glare border separating bento cards
val ErrorRed = Color(0xFFFF4D4D)           // High visibility alarm red
val ErrorRedDim = Color(0x26FF4D4D)        // Translucent danger card layer
val NeonMintTranslucent = Color(0x2600FFCC) // Translucent light neon card container label bg
val ObsidianPure = Color(0xFF020304)

// Sleek Neo-Bento Light Mode Vibe
val BentoLightBg = Color(0xFFF5F7FA)       // Crisp clean cool-gray background
val BentoCardWhite = Color(0xFFFFFFFF)     // Clean white bento box card surfaces
val BentoCardLightGlow = Color(0xFFEDF1F6) // Elevated light depth
val NeonTealLight = Color(0xFF00A587)      // Darker rich mint suited for light mode readability
val SoftCyanLight = Color(0xFF00899B)      // Subtler cyan for light mode
val DarkCharcoalText = Color(0xFF13161C)   // Deep charcoal primary typography
val MutedSlateText = Color(0xFF6B7280)     // Muted gray for description text
val LightBorder = Color(0xFFE2E8F0)        // Subtle micro-border separating light bento cards
val LightTranslucentTeal = Color(0x1A00A587)

